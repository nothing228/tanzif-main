# Copy to .env for local development with `netlify dev`.
# In production set this in Netlify: Site configuration → Environment variables.
# Never commit the real key, and never prefix it with VITE_ — anything VITE_*
# is compiled into the browser bundle and would be public.
ANTHROPIC_API_KEY=sk-ant-...
