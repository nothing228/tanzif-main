[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# SPA routing — everything that isn't a function or a real file serves index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
